import React from 'react';
import { PlaceholderPage } from '../PlaceholderPage';

export function ResidentesPage() {
  return (
    <PlaceholderPage
      title="Gestión de Residentes"
      description="Módulo para administrar residentes dentro del residencial. Pronto podrás crear, editar y gestionar los residentes asignados a cada unidad."
    />
  );
}
